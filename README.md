Executing `npm test` results in the output `!!!helper executed!!!` and both tests execute without failure.

However, executing `karma start --single-run` will not print the message and the second test fails.